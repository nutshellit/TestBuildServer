To build projects and execute tests run build_test.cmd from a command console.

There is also a watchtest.cmd file in the Atm.Tests directory to automatically run tests from console when any changes to code in test project are detected.

